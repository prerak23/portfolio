import React from 'react';

import Section from '../section';

const SectionResearchInterest = ({ about }) => {
  return (
    <Section title="Research Interests">
      <div className="mb-6">
        <p>Audio Signal Processing, Acoustics, Natural Language Processing, Bayesian Inference, Deep Neural Networks, Machine Learning</p>
      </div>
    </Section>
  );
};

export default SectionResearchInterest;
